"""Auto-imported by Python's `site` machinery when this package's directory
is on `PYTHONPATH` - see cli.py for how `run()` arranges that. This is what
actually runs `initialize()` before the target application's own code.
"""

from otelfeature_instrument.bootstrap import initialize

initialize()
